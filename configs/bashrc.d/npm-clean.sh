alias "npm-clean"="rm -rf node_modules package-lock.json yarn.lock pnpm-lock.yaml && npm i";
alias "yarn-clean"="rm -rf node_modules package-lock.json yarn.lock pnpm-lock.yaml && yarn";
alias "pnpm-clean"="rm -rf node_modules package-lock.json yarn.lock pnpm-lock.yaml && pnpm i";
alias "npm-clean-lite"="rm -rf node_modules && npm i";
